package io.github.youndie.mani.feature.health

/**
 * Отвечает ли хранилище.
 *
 * Порт, как и репозитории: официальный драйвер существует только на JVM, mongkn — только под
 * linuxX64, и «сходить в базу» у них разное. Общего здесь ровно одно — вопрос.
 *
 * Реализация обязана СХОДИТЬ в базу, а не вернуть закэшированное состояние клиента: драйвер
 * считает соединение живым до первой неудачной операции, и проба на таком ответе зеленела бы
 * при мёртвой базе.
 */
fun interface StorageHealth {
    suspend fun isReachable(): Boolean
}
